[project]
name = "admitflow-ai"
version = "0.1.0"
description = "College application organizer with AI-assisted workflows."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
admitflow-ai = "admitflow_ai.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

